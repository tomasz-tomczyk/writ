# Competitive comparison matrix

Objective, per-tool notes live in `vs-<slug>.md`. Values below are drawn from
those briefs; cells say "Unknown" only where the brief explicitly marks a fact
as unknown.

| Dimension | writ | mem0 | cursor-memories-rules | claude-code-memory | aider | continue-dev | windsurf | zep-letta | langmem |
|---|---|---|---|---|---|---|---|---|---|
| Local-first / data leaves machine | Local SQLite under XDG paths; no server, account, or telemetry. | Partial; local/self-hosted options exist, managed cloud stores data with mem0. | Partial; project rules are local files, but user/team rules and indexing pass through Cursor cloud. | Partial; project `CLAUDE.md` and auto memory are local, but cloud features pass through Anthropic servers. | Partial; tool and history are local, but prompts go to the user's LLM provider. | Partial; open-source core runs locally; Continue Hub was cloud-hosted and is sunsetting. | Partial; memories and workspace rules are local, but inference/cloud pass through Cognition/Codeium. | Partial; self-hosting available, but both Zep and Letta offer managed cloud/SaaS. | Partial; local in-process and self-hosted options; managed service is an interest form. |
| Durable rules with rationale | Yes; each learning requires a `rationale`. | No; stores extracted facts/preferences without required rationale. | No; free-form rules/prompts without required rationale. | Partial; `CLAUDE.md` instructions are human-authored, but no required rationale field. | Partial; `CONVENTIONS.md` encodes preferences, but no structured rule/rationale entity. | Partial; rules are human-authored markdown, but no required rationale field. | Partial; rules are human-authored, but no required rationale field. | No; extracted facts/Observations/memory blocks, not rationale-bearing rules. | No; extracted facts/profiles/episodes without required rationale. |
| Human approve before active | Yes; `proposed` → `active` gate. | No; memories enter retrieval once added. | No; saved project rules are active immediately. Removed Memories required approval. | No; committed `CLAUDE.md` rules are active immediately. | No; loaded convention rules are live immediately. | No; a rule file is active as soon as it exists. | No; saved workspace rules are active immediately. | No; memories enter retrieval on ingest. | No; memories enter retrieval once written. |
| Scope model | `global`, `project:`, `language:`, `glob:`; AND across kinds, OR within a kind. | Multi-tenant by `user_id`, `run_id`, `agent_id`, `org_id`; not rule-level scopes. | `globs` + always/intelligent/manual modes; no project/language/repo-identity scopes. | Hierarchical load order + `paths:`/`globs:`; no repo-identity or language scopes. | Implicit project-root/per-directory config or manual `/read`; no formal scopes. | `globs:`/`regex:` + `alwaysApply`; no repo-identity or language scopes. | `trigger` frontmatter + `AGENTS.md` directory scoping; no repo-identity/language scopes. | Zep: multi-tenant user/customer/session; Letta: per-agent blocks. Neither matches writ. | Hierarchical namespaces + metadata filters; not project/language/glob. |
| Diff-time selection / audit | Yes; selects active scoped rules against the diff, ranks, caps, and emits a prompt asking for findings. | No; semantic + BM25 + entity retrieval to query. | No; rules injected into system prompt based on context. | No; instructions injected into context. | No; convention files included whole in chat context. | No; rules concatenated into the system message. | No; rules injected based on triggers/context. | No; semantic/keyword/graph retrieval or memory blocks/tools. | No; semantic/metadata retrieval. |
| Prune / archive | Yes; `writ archive` retires a rule without deleting it. | Unknown; ADD-only with memory-decay soft re-ranking; confirmed archive not documented. | No; delete file or dashboard rule; no archive. | No; edit/delete files or memory notes; no documented archive. | No; edit convention files by hand. | No; delete or edit rule files. | No; delete rule/memory files or use UI. | Unknown; temporal invalidation/retention for Zep, git version history for Letta. | Unknown; update/delete/consolidate configurable; confirmed archive not documented. |
| UI for collection | Yes; Inbox, Collection, Detail, Health screens. | Yes; dashboard (cloud) and local dashboard for OpenMemory MCP. | Partial; Customize → Rules UI, no collection-health view. | Partial; `/memory` command and file editor, no dedicated collection-health UI. | No; plain-text files only; optional browser UI is for chat. | Partial; file editor and IDE toolbar; no collection-health UI. | Partial; Customizations panel for memories/rules, no collection-health view. | Yes; Zep Cloud dashboard/Graphiti custom; Letta ADE/desktop app. | No; open-source SDK has no curation UI. |
| Terminal findings | Yes; audit emits findings in the terminal; UI owns the collection. | No; retrieval layer feeding agent context. | No; hooks observe/block but do not ingest rule violations. | No; no structured findings/ingest flow. | No; emits edits and lint/test output, not structured findings. | No; no structured findings/ingest flow. | No; hooks are host stop messages, not rule-violation ingestion. | No; both feed context into the agent. | No; retrieval and prompt-optimization layer. |
| Multi-agent host neutrality | Yes; MCP + `writ install` for Claude Code, Codex, Cursor, OpenCode. | Broad; SDK/framework integrations + MCP for coding agents. | No; Cursor-only. | No; Claude-specific. | No; Aider is itself the agent. | No; Continue-specific. | No; Windsurf/Cascade-only. | Partial; Zep MCP + frameworks; Letta agent harness with MCP. Both broader than writ's coding-host focus. | Partial; storage-agnostic core but LangGraph-native; Python SDK. |
| Open source | Yes. | Yes; Apache 2.0 library/server, optional managed cloud. | No; proprietary. | No; proprietary. | Yes; Apache 2.0. | Yes; Apache 2.0, but upstream repository is read-only and unmaintained. | No; proprietary. | Partial; Zep Graphiti MIT, Letta Code Apache 2.0; clouds proprietary. | Yes; MIT. |
| Pricing posture | Free, self-hosted; no paid tiers planned for core. | Freemium managed cloud + free self-hosted open source. | Freemium IDE/agent with paid Pro/Teams/Enterprise tiers. | Freemium chat / paid Pro/Max/Team/Enterprise; Claude Code requires paid plan. | Free CLI; user pays own LLM usage. | Core free/open source; Continue Hub paid tiers sunsetting. | Freemium with Free/Pro/Max/Teams/Enterprise tiers. | Zep: freemium cloud + free self-assembled Graphiti; Letta: free self-host + paid cloud. | Free open-source SDK; managed service is an interest form. |

## Sources / Notes

- Per-tool briefs: [`vs-writ.md`](vs-writ.md), [`vs-mem0.md`](vs-mem0.md),
  [`vs-cursor-memories.md`](vs-cursor-memories.md),
  [`vs-claude-code.md`](vs-claude-code.md), [`vs-aider.md`](vs-aider.md),
  [`vs-continue-dev.md`](vs-continue-dev.md), [`vs-windsurf.md`](vs-windsurf.md),
  [`vs-zep-letta.md`](vs-zep-letta.md), [`vs-langmem.md`](vs-langmem.md).
- "Partial" means the capability exists in some form but does not match writ's
  specific model; see the per-tool brief for the exact limitation.
- "Unknown" is used only where the source brief explicitly marks a fact as
  unknown.
- Research dates and primary sources are listed in each `vs-<slug>.md` file.
